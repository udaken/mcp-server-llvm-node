{
  "mcpServers": {
    "llvm-mcp-server": {
      "command": "docker",
      "args": [
        "compose",
        "-f", "docker-compose.yml",
        "run",
        "--rm",
        "--service-ports",
        "--name", "llvm-mcp-server",
        "llvm-mcp-server"
      ],
      "env": {
        "NODE_ENV": "production"
        }
    }
  }
}